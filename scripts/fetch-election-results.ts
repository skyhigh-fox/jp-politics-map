/**
 * 選挙結果（簡易版）取得スクリプト — 未実装スタブ。
 *
 * データソース: 総務省「選挙関連資料」
 *   https://www.soumu.go.jp/senkyo/senkyo_s/data/
 *   （例: 第50回衆院選 https://www.soumu.go.jp/senkyo/senkyo_s/data/shugiin50/index.html ）
 *
 * 現状の課題（2026-08-10 データソース調査時点）:
 *   - 候補者別得票数はExcelファイルでのみ配布されており、CSV/HTML表がない
 *   - ファイルが選挙区ごとに多数に分かれており、レイアウトも年度・選挙区で
 *     揺れがある可能性がある（未確認）
 *   - 自動化前に、実際に数ファイルをダウンロードしてシート構成・セル配置を
 *     目視確認する必要がある（Node.js/Excel閲覧環境が整い次第、要調査）
 *
 * 方針:
 *   1. まず候補となるExcelファイルを1〜2件手動確認し、共通レイアウトを特定する
 *   2. `xlsx` (SheetJS) で読み込み、Legislator.name と突合するための
 *      候補者名の表記ゆれ対策（全角/半角、旧字体等）を検討する
 *   3. 選挙区単位でファイルが分かれているため、都道府県・選挙区名から
 *      対象ファイルを機械的に特定するマッピング表を別途作る
 *
 * 上記が固まるまでは、このスクリプトは何もしない（フェーズ1の他データより
 * 後回しにする、という合意どおりの状態）。
 */

async function main() {
  console.log(
    "fetch-election-results はまだ未実装です。実際のExcelファイルのレイアウトを確認してから実装してください。"
  );
}

main();
